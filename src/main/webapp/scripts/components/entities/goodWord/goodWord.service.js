'use strict';

angular.module('polcoApp')
    .factory('GoodWord', function ($resource) {
        return $resource('api/goodWords/:id', {}, {
            'query': { method: 'GET', isArray: true},
            'get': {
                method: 'GET',
                transformResponse: function (data) {
                    data = angular.fromJson(data);
                    return data;
                }
            },
            'update': { method:'PUT' }
        });
    });
