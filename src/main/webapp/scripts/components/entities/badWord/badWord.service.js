'use strict';

angular.module('polcoApp')
    .factory('BadWord', function ($resource) {
        return $resource('api/badWords/:id', {}, {
            'query': { method: 'GET', isArray: true},
            'get': {
                method: 'GET',
                transformResponse: function (data) {
                    data = angular.fromJson(data);
                    return data;
                }
            },
            'update': { method:'PUT' }
        });
    });
